package com.linebattle.setup

import org.scalatest._

class SetupTest extends FunSpec with Matchers {

  /*
	describe( "The setup" ) {
		
		it( "should return always the parameter + 1" ) {
		
			Setup.testFunction( 1 ) should be ( 2 )
		}
		
		it( "should throw a NoSuchElementException if the parameter is < 0" ) {
			a [NoSuchElementException] should be thrownBy {
				Setup.testFunction( - 1 )
			}
		}
	}	
	*
	*/
}